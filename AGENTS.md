# 권쌤의 교실 프로젝트 가이드

## 프로젝트 개요

- 프로젝트 이름: 권쌤의 교실
- 목적: 교사 프로필, 교실 갤러리, 발표 순서 웹앱을 한 페이지에서 보여주는 사이트
- 기술 구성: HTML, CSS, JavaScript를 `index.html` 한 파일 안에서 사용
- 외부 서비스: Supabase Storage의 공개 이미지 URL 사용, Vercel에 배포

## 기존 기능

- 프로필
- 자기소개
- 관심 분야
- 교실 갤러리
- 발표 순서 정하기
- 연락 영역

## 작업 원칙

0. 작업을 시작할 때 프로젝트 루트의 `memory.md`가 있으면 먼저 읽고, 지난 작업과 다음 확인 사항을 파악한다.
1. 수정 전에 계획을 먼저 설명한다.
2. 요청하지 않은 기능은 추가하지 않는다.
3. 기존 기능을 삭제하지 않는다.
4. 필요한 부분만 최소한으로 수정한다.
5. 모바일과 데스크톱 화면을 함께 확인한다.
6. 비밀 키와 `service_role` 키는 코드에 넣지 않는다.
7. 수정 후 확인 방법을 한국어로 설명한다.

## 위험한 명령 안전 규칙

- 파일이나 폴더를 삭제하는 명령은 실행 전에 대상과 이유를 사용자에게 설명하고 승인을 받는다.
- `git reset --hard`, `git clean -fd`, `rm -rf` 같은 복구하기 어려운 명령은 사용자 승인 없이 실행하지 않는다.
- Vercel 프로젝트 삭제, Supabase 데이터 삭제, Storage 파일 삭제는 반드시 먼저 사용자에게 확인한다.
- 비밀 키, 토큰, 비밀번호를 터미널 출력이나 코드에 노출하지 않는다.
- 명령의 영향 범위가 불분명하면 실행하지 말고 먼저 질문한다.

## 수정 후 검증 체크리스트

- 프로필, 자기소개, 관심 분야, 교실 갤러리, 발표 순서 정하기, 연락 영역이 그대로 있는지 확인한다.
- 발표 순서 정하기에서 예시 입력, 순서 뽑기, 초기화 기능이 정상 작동하는지 확인한다.
- 상단 메뉴와 버튼을 눌렀을 때 해당 영역으로 정상 이동하는지 확인한다.
- 데스크톱과 모바일 화면에서 글자, 사진, 카드가 겹치거나 화면 밖으로 나가지 않는지 확인한다.
- Supabase 공개 이미지가 정상적으로 표시되는지 확인한다.
- `service_role` 키, 비밀번호, 개인 액세스 토큰 같은 비밀 정보가 코드에 포함되지 않았는지 확인한다.
- 수정된 파일만 확인하고, 요청하지 않은 변경이 없는지 `git diff`로 확인한다.
